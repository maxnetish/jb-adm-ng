import { PostContentTypeDisplayPipe } from './post-content-type-display.pipe';

describe('PostContentTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PostContentTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
