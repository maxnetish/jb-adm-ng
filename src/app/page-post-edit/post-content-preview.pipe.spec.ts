import { PostContentPreviewPipe } from './post-content-preview.pipe';

describe('PostContentPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new PostContentPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
