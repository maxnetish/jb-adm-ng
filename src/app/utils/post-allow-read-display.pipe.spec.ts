import { PostAllowReadDisplayPipe } from './post-allow-read-display.pipe';

describe('PostAllowReadDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PostAllowReadDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
