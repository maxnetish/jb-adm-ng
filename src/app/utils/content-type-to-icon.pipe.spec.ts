import { ContentTypeToIconPipe } from './content-type-to-icon.pipe';

describe('ContentTypeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
