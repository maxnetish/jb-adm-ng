import { JbFileInputDirective } from './jb-file-input.directive';

describe('JbFileInputDirective', () => {
  it('should create an instance', () => {
    const directive = new JbFileInputDirective();
    expect(directive).toBeTruthy();
  });
});
